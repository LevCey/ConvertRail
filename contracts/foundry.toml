[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200

[lint]
exclude_lints = ["screaming-snake-case-immutable"]

[rpc_endpoints]
arc_testnet = "${ARC_TESTNET_RPC_URL}"
